# API サーバーの baseURL
# 開発（Prism モック）: /api  ← Vite proxy 経由で localhost:4010 に転送（ブラウザ・エミュレータ共通）
# 本番: https://api.example.com/api
# .env.development / .env.production を作成してここからコピーして使用
VITE_API_BASE_URL=/api
